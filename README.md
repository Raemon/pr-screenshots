# pr-screenshots

Public image host for PR screenshots taken by Claude Code, so they render in PR descriptions of private repos and on humanintheloop.reviews. Layout: `<repo>/<branch>/<name>.png`. Uploaded via the GitHub contents API; see the "PR screenshots" rule in ~/.claude/CLAUDE.md.
